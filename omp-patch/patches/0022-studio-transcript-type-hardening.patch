diff --git a/packages/coding-agent/src/studio/bridge-server.ts b/packages/coding-agent/src/studio/bridge-server.ts
index 9195495..ef89b9a 100644
--- a/packages/coding-agent/src/studio/bridge-server.ts
+++ b/packages/coding-agent/src/studio/bridge-server.ts
@@ -202,7 +202,7 @@ export class StudioBridgeServer implements StudioBridgeLifecycle {
 					type: "studio.hello.result",
 					requestId: hello.requestId,
 					selectedProtocolVersion: STUDIO_PROTOCOL_VERSION,
-					runtimeVersion: `${VERSION}-studio.21`,
+					runtimeVersion: `${VERSION}-studio.22`,
 					upstreamVersion: VERSION,
 					upstreamCommit: UPSTREAM_COMMIT,
 					runtimeInstanceId: runtime.runtimeId,
diff --git a/packages/coding-agent/src/studio/services/session-transcript-service.ts b/packages/coding-agent/src/studio/services/session-transcript-service.ts
index 7c3d746..506842d 100644
--- a/packages/coding-agent/src/studio/services/session-transcript-service.ts
+++ b/packages/coding-agent/src/studio/services/session-transcript-service.ts
@@ -102,7 +102,10 @@ function mapContentBlock(block: unknown): ConversationContentBlock | undefined {
 	return undefined;
 }
 
-function mapToolResultMessage(message: Record<string, unknown>, fallbackName?: string): ConversationContentBlock {
+function mapToolResultMessage(
+	message: Record<string, unknown>,
+	fallbackName?: string,
+): Extract<ConversationContentBlock, { type: "toolResult" }> {
 	const toolCallId =
 		typeof message.toolCallId === "string" && message.toolCallId.length > 0
 			? message.toolCallId.slice(0, CONVERSATION_LIMITS.ITEM_ID_MAX_CHARS)
@@ -125,7 +128,7 @@ function mapToolResultMessage(message: Record<string, unknown>, fallbackName?: s
 	}
 	const output = sanitizePublicText(texts.join("\n"), CONVERSATION_LIMITS.TEXT_BLOCK_MAX_BYTES);
 	truncated ||= output.truncated || omittedBinary;
-	const mapped: ConversationContentBlock = {
+	const mapped: Extract<ConversationContentBlock, { type: "toolResult" }> = {
 		type: "toolResult",
 		toolCallId,
 		isError: message.isError === true,
@@ -245,7 +248,11 @@ function projectBranch(entries: readonly SessionEntry[]): ConversationItem[] {
 	const orphans: typeof toolResults = [];
 	for (const { entry, result } of toolResults) {
 		const ownerIndex = toolOwners.get(result.toolCallId);
-		const owner = ownerIndex === undefined ? undefined : items[ownerIndex];
+		if (ownerIndex === undefined) {
+			orphans.push({ entry, result });
+			continue;
+		}
+		const owner = items[ownerIndex];
 		if (owner === undefined || owner.kind !== "message") {
 			orphans.push({ entry, result });
 			continue;
