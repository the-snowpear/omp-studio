diff --git a/packages/coding-agent/package.json b/packages/coding-agent/package.json
index 7620602317..f8a9c6b47f 100644
--- a/packages/coding-agent/package.json
+++ b/packages/coding-agent/package.json
@@ -511,6 +511,10 @@
       "types": "./src/session/*.ts",
       "import": "./src/session/*.ts"
     },
+    "./studio/*": {
+      "types": "./src/studio/*.ts",
+      "import": "./src/studio/*.ts"
+    },
     "./slash-commands/*": {
       "types": "./src/slash-commands/*.ts",
       "import": "./src/slash-commands/*.ts"
diff --git a/packages/coding-agent/src/cli.ts b/packages/coding-agent/src/cli.ts
index d8be66b822..dffb4cf9fe 100755
--- a/packages/coding-agent/src/cli.ts
+++ b/packages/coding-agent/src/cli.ts
@@ -433,6 +433,14 @@ export async function runCli(argv: string[]): Promise<void> {
 		process.stdout.write(formatLicenseOutput());
 		return;
 	}
+	// Hidden Studio archived-session telemetry probe: must stay ahead of
+	// command/launch dispatch so the request JSON on stdin is never treated as
+	// a prompt, and must never appear in public help output.
+	if (resolvedArgv[0] === "--studio-session-telemetry-probe") {
+		const { runStudioSessionTelemetryProbe } = await import("./studio/session-telemetry-probe");
+		process.exitCode = await runStudioSessionTelemetryProbe();
+		return;
+	}
 	let stopStartupComposer: (() => void) | undefined;
 	if (
 		!process.env.PI_TIMING &&
diff --git a/packages/coding-agent/src/cli/args.ts b/packages/coding-agent/src/cli/args.ts
index 412e782895..070f79a7b8 100644
--- a/packages/coding-agent/src/cli/args.ts
+++ b/packages/coding-agent/src/cli/args.ts
@@ -20,7 +20,7 @@ import { CliUsageError } from "./usage-error";
 
 export { getExtraHelpText };
 
-export type Mode = "text" | "json" | "rpc" | "acp" | "rpc-ui";
+export type Mode = "text" | "json" | "rpc" | "acp" | "rpc-ui" | "studio-host";
 
 export interface Args {
 	cwd?: string;
@@ -56,6 +56,9 @@ export interface Args {
 	help?: boolean;
 	version?: boolean;
 	mode?: Mode;
+	bridgeEndpoint?: string;
+	bridgeTokenFile?: string;
+	bridgeRuntimeEpoch?: number;
 	noSession?: boolean;
 	sessionDir?: string;
 	providerSessionId?: string;
diff --git a/packages/coding-agent/src/cli/flag-tables.ts b/packages/coding-agent/src/cli/flag-tables.ts
index c1f6e46edd..abf0db7964 100644
--- a/packages/coding-agent/src/cli/flag-tables.ts
+++ b/packages/coding-agent/src/cli/flag-tables.ts
@@ -104,6 +104,14 @@ function parseMaxTimeSeconds(value: string): number {
 	);
 }
 
+function parseBridgeRuntimeEpoch(value: string): number {
+	const epoch = Number(value);
+	if (Number.isSafeInteger(epoch) && epoch > 0) return epoch;
+	throw new CliUsageError(
+		`Invalid --bridge-runtime-epoch value: ${JSON.stringify(value)}. Expected a positive safe integer.`,
+	);
+}
+
 /**
  * Setters for flags with string values. Most built-ins consume the next argv
  * token even when it starts with `-`; flags listed in
@@ -121,10 +129,26 @@ export const STRING_SETTERS: Record<string, StringSetter> = {
 		result.addDir = [...(result.addDir ?? []), value];
 	},
 	"--mode": (result, value) => {
-		if (value === "text" || value === "json" || value === "rpc" || value === "acp" || value === "rpc-ui") {
+		if (
+			value === "text" ||
+			value === "json" ||
+			value === "rpc" ||
+			value === "acp" ||
+			value === "rpc-ui" ||
+			value === "studio-host"
+		) {
 			result.mode = value;
 		}
 	},
+	"--bridge-endpoint": (result, value) => {
+		result.bridgeEndpoint = value;
+	},
+	"--bridge-token-file": (result, value) => {
+		result.bridgeTokenFile = value;
+	},
+	"--bridge-runtime-epoch": (result, value) => {
+		result.bridgeRuntimeEpoch = parseBridgeRuntimeEpoch(value);
+	},
 	"--fork": (result, value) => {
 		result.fork = value;
 	},
diff --git a/packages/coding-agent/src/commands/launch-help.ts b/packages/coding-agent/src/commands/launch-help.ts
index 1a21093033..03f5019ed9 100644
--- a/packages/coding-agent/src/commands/launch-help.ts
+++ b/packages/coding-agent/src/commands/launch-help.ts
@@ -40,8 +40,17 @@ export const launchHelp = {
 		alias: Flags.string({ description: "Create a shell shortcut for the selected profile and exit" }),
 		cwd: Flags.string({ description: "Directory to start in (overrides the launch cwd)" }),
 		mode: Flags.string({
-			description: "Output mode: text (default), json, rpc, or rpc-ui",
-			options: ["text", "json", "rpc", "acp", "rpc-ui"],
+			description: "Output mode: text (default), json, rpc, acp, rpc-ui, or studio-host",
+			options: ["text", "json", "rpc", "acp", "rpc-ui", "studio-host"],
+		}),
+		"bridge-endpoint": Flags.string({
+			description: "Studio Bridge endpoint (transport is enabled by the Studio host protocol patch)",
+		}),
+		"bridge-token-file": Flags.string({
+			description: "Studio Bridge authentication token file",
+		}),
+		"bridge-runtime-epoch": Flags.string({
+			description: "Positive Thread epoch assigned to this Studio Runtime process",
 		}),
 		config: Flags.string({
 			description: "Load an extra config.yml-style overlay for this run (repeatable)",
diff --git a/packages/coding-agent/src/config/settings-schema.ts b/packages/coding-agent/src/config/settings-schema.ts
index bdad59976a..f6da2a3158 100644
--- a/packages/coding-agent/src/config/settings-schema.ts
+++ b/packages/coding-agent/src/config/settings-schema.ts
@@ -5069,7 +5069,7 @@ export const SETTINGS_SCHEMA = {
 
 	"task.agentIdleTtlMs": {
 		type: "number",
-		default: 420_000,
+		default: 300_000,
 		ui: {
 			tab: "tasks",
 			group: "Subagents",
diff --git a/packages/coding-agent/src/main.ts b/packages/coding-agent/src/main.ts
index 71db367f9b..1c997568f0 100644
--- a/packages/coding-agent/src/main.ts
+++ b/packages/coding-agent/src/main.ts
@@ -97,15 +97,22 @@ import {
 	persistForeignSession,
 } from "./session/foreign-session-import";
 import type { ForeignSessionInfo, ForeignSessionSource, ForeignSessionStore } from "./session/foreign-session-store";
+import { STUDIO_HOST_SESSION_ORIGIN } from "./session/session-entries";
 import { resolveResumableSession, type SessionInfo } from "./session/session-listing";
 import { SessionManager } from "./session/session-manager";
 import { executeBuiltinSlashCommand } from "./slash-commands/builtin-registry";
 import { shouldShowStartupSplash } from "./startup-splash";
+import type { StudioLoopService } from "./studio/services/loop-service";
+import type { StudioModeControlService } from "./studio/services/mode-control-service";
+import type { StudioHostRuntime } from "./studio/studio-host-mode";
+import { runStudioHostMode as defaultRunStudioHostMode, type RunStudioHostMode } from "./studio/studio-host-mode";
 import { discoverTitleSystemPromptFile, resolvePromptInput } from "./system-prompt";
+import { createMCPProxyTools } from "./task/executor";
 import { createPersistedSubagentReviverFactory } from "./task/persisted-revive";
 import { createTelemetryExportConfig, initTelemetryExport, isTelemetryExportEnabled } from "./telemetry-export";
 import { concreteThinkingLevel, parseConfiguredThinkingLevel } from "./thinking";
 import type { LspStartupServerInfo } from "./tools";
+import type { ToolUiFactory } from "./tools/context";
 import { getChangelogPath, resolveStartupChangelogForDisplay, type StartupChangelogSelection } from "./utils/changelog";
 import { EventBus } from "./utils/event-bus";
 
@@ -113,7 +120,7 @@ type RunAcpMode = (createSession: AcpSessionFactory) => Promise<never>;
 type RunPrintMode = (session: AgentSession, options: PrintModeOptions) => Promise<void>;
 type RunRpcMode = (
 	session: AgentSession,
-	setToolUIContext?: (uiContext: ExtensionUIContext, hasUI: boolean) => void,
+	setToolUIContext?: (uiContext: ExtensionUIContext | ToolUiFactory | undefined, hasUI: boolean) => void,
 	subagentEventBus?: EventBus,
 	input?: ReadableStream<Uint8Array>,
 ) => Promise<never>;
@@ -488,7 +495,7 @@ async function runInteractiveMode(
 	notifs: (InteractiveModeNotify | null)[],
 	versionCheckPromise: Promise<string | undefined>,
 	initialMessages: string[],
-	setExtensionUIContext: (uiContext: ExtensionUIContext, hasUI: boolean) => void,
+	setExtensionUIContext: (uiContext: ExtensionUIContext | ToolUiFactory | undefined, hasUI: boolean) => void,
 	lspServers: LspStartupServerInfo[] | undefined,
 	mcpManager: MCPManager | undefined,
 	resuming: boolean,
@@ -501,6 +508,9 @@ async function runInteractiveMode(
 	joinLink?: string,
 	startBackgroundModelDiscovery?: () => Promise<void>,
 	startupLease?: ComposerLease,
+	studioLoopService?: StudioLoopService,
+	studioModeService?: StudioModeControlService,
+	studioShutdownRequested?: Promise<void>,
 ): Promise<void> {
 	let mode: InteractiveMode;
 	try {
@@ -514,6 +524,8 @@ async function runInteractiveMode(
 			eventBus,
 			startupLease?.composer,
 			subagentEventBus,
+			studioLoopService,
+			studioModeService,
 		);
 		startupLease?.adopt();
 	} catch (error) {
@@ -632,8 +644,20 @@ async function runInteractiveMode(
 	}
 
 	while (true) {
-		const input = await mode.getUserInput();
-		await submitInteractiveInput(mode, session, input);
+		if (studioShutdownRequested === undefined) {
+			const input = await mode.getUserInput();
+			await submitInteractiveInput(mode, session, input);
+			continue;
+		}
+		const next = await Promise.race([
+			mode.getUserInput().then(input => ({ kind: "input" as const, input })),
+			studioShutdownRequested.then(() => ({ kind: "shutdown" as const })),
+		]);
+		if (next.kind === "shutdown") {
+			await mode.shutdown();
+			return;
+		}
+		await submitInteractiveInput(mode, session, next.input);
 	}
 }
 
@@ -944,13 +968,14 @@ export async function createSessionManager(
 	activeSettings: Settings = settings,
 	askToMoveSession: SessionPrompt = promptMoveSession,
 ): Promise<SessionManager | undefined> {
+	const studioOrigin = parsed.mode === "studio-host" ? STUDIO_HOST_SESSION_ORIGIN : undefined;
 	if (parsed.fork) {
 		if (parsed.noSession) {
 			throw new SessionResolutionError("--fork requires session persistence");
 		}
 		const forkSource = parsed.fork;
 		if (forkSource.includes("/") || forkSource.includes("\\") || forkSource.endsWith(".jsonl")) {
-			return await SessionManager.forkFrom(forkSource, cwd, parsed.sessionDir);
+			return await SessionManager.forkFrom(forkSource, cwd, parsed.sessionDir, undefined, { studioOrigin });
 		}
 		const match = await resolveResumableSession(forkSource, cwd, parsed.sessionDir);
 		if (!match) {
@@ -959,7 +984,7 @@ export async function createSessionManager(
 				"Run `omp --resume` without an argument to pick from recent sessions, or `omp` to start a new one.",
 			);
 		}
-		return await SessionManager.forkFrom(match.session.path, cwd, parsed.sessionDir);
+		return await SessionManager.forkFrom(match.session.path, cwd, parsed.sessionDir, undefined, { studioOrigin });
 	}
 
 	if (parsed.noSession) {
@@ -1012,26 +1037,26 @@ export async function createSessionManager(
 		return await SessionManager.open(match.session.path, parsed.sessionDir);
 	}
 	if (parsed.continue) {
-		return await SessionManager.continueRecent(cwd, parsed.sessionDir);
+		return await SessionManager.continueRecent(cwd, parsed.sessionDir, undefined, { studioOrigin });
 	}
 	// --resume without value is handled separately (needs picker UI)
 	// If --session-dir provided without --continue/--resume, create new session there
 	if (parsed.sessionDir) {
-		return SessionManager.create(cwd, parsed.sessionDir);
+		return SessionManager.create(cwd, parsed.sessionDir, undefined, { studioOrigin });
 	}
 	// Auto-resume: behave like --continue if the setting is enabled and a prior
 	// session exists. When a prior session is resumed, mark parsed.continue so
 	// buildSessionOptions restores the session's model/thinking instead of
 	// overriding them with CLI defaults.
 	if (activeSettings.get("autoResume")) {
-		const manager = await SessionManager.continueRecent(cwd, parsed.sessionDir);
+		const manager = await SessionManager.continueRecent(cwd, parsed.sessionDir, undefined, { studioOrigin });
 		if (manager.getEntries().length > 0) {
 			parsed.continue = true;
 		}
 		return manager;
 	}
-	// Default case (new session) returns undefined, SDK will create one
-	return undefined;
+	// Default Studio startup creates the manager here so its first header carries the origin marker.
+	return studioOrigin ? SessionManager.create(cwd, parsed.sessionDir, undefined, { studioOrigin }) : undefined;
 }
 
 /** Discover SYSTEM.md file if no CLI system prompt was provided */
@@ -1389,6 +1414,7 @@ interface RunRootCommandDependencies {
 	discoverAuthStorage?: typeof discoverAuthStorage;
 	selectSession?: typeof selectSession;
 	runAcpMode?: RunAcpMode;
+	runStudioHostMode?: RunStudioHostMode;
 	createForeignSessionStore?: (source: ForeignSessionSource) => ForeignSessionStore;
 	settings?: Settings;
 	forceSetupWizard?: boolean;
@@ -1443,6 +1469,10 @@ export async function runRootCommand(
 			process.exit(1);
 		}
 		const mode = parsedArgs.mode || "text";
+		if (mode === "studio-host" && parsedArgs.print) {
+			process.stderr.write(`${chalk.red("Error: --print is not supported in studio-host mode")}\n`);
+			process.exit(1);
+		}
 		// RPC owns stdin. Claim its singleton stream before plugin/extension discovery can load an in-process consumer.
 		const rpcInput = mode === "rpc" || mode === "rpc-ui" ? claimRpcInput() : undefined;
 
@@ -1477,10 +1507,12 @@ export async function runRootCommand(
 		// session-critical database connection picks the right busy timeout.
 		// See getDbBusyTimeoutMs().
 		const isProtocolMode = mode === "rpc" || mode === "rpc-ui" || mode === "acp";
+		const isStudioHost = mode === "studio-host";
 		// Protocol modes own stdin; treating it as prompt text would consume JSON-RPC frames before their transports start.
-		const pipedInput = isProtocolMode ? undefined : await logger.time("readPipedInput", readPipedInput);
+		const pipedInput =
+			isProtocolMode || isStudioHost ? undefined : await logger.time("readPipedInput", readPipedInput);
 		const autoPrint = pipedInput !== undefined && !parsedArgs.print && parsedArgs.mode === undefined;
-		const isInteractive = !parsedArgs.print && !autoPrint && parsedArgs.mode === undefined;
+		const isInteractive = !parsedArgs.print && !autoPrint && (parsedArgs.mode === undefined || isStudioHost);
 		// Only the interactive host renders a focusable Agent Hub / subagent session
 		// tree; declare it so headless subagent optimizations (e.g. skipping replan
 		// title refresh) can tell a focusable process from a print/RPC/eval one.
@@ -1506,7 +1538,6 @@ export async function runRootCommand(
 			process.stderr.write(`${chalk.red(`Error: ${message}`)}\n`);
 			process.exit(1);
 		}
-
 		const settingsInstance = await settingsPromise;
 		if (parsedArgs.approvalMode) {
 			// Runtime override (not persisted): every settings.get("tools.approvalMode") downstream
@@ -1705,6 +1736,9 @@ export async function runRootCommand(
 			}
 			throw error;
 		}
+		if (isStudioHost && sessionManager) {
+			sessionManager.setNewSessionStudioOrigin(STUDIO_HOST_SESSION_ORIGIN);
+		}
 
 		if ((typeof parsedArgs.resume === "string" || foreignSource) && sessionManager) {
 			const previousCwd = cwd;
@@ -1999,7 +2033,7 @@ export async function runRootCommand(
 					eventBus,
 					subagentEventBus,
 				}),
-				Math.trunc(Number(settingsInstance.get("task.agentIdleTtlMs") ?? 420_000) || 0),
+				Math.trunc(Number(settingsInstance.get("task.agentIdleTtlMs") ?? 300_000) || 0),
 			);
 			if (parsedArgs.apiKey && !sessionOptions.model && session.model) {
 				authStorage.setRuntimeApiKey(session.model.provider, parsedArgs.apiKey);
@@ -2072,31 +2106,84 @@ export async function runRootCommand(
 				try {
 					stopStartupWatchdog();
 					logger.endTiming();
-					await runInteractiveMode(
-						session,
-						VERSION,
-						startupChangelog,
-						notifs,
-						versionCheckPromise,
-						initialArgs.messages,
-						setToolUIContext,
-						lspServers,
-						mcpManager,
-						Boolean(parsedArgs.continue || parsedArgs.resume || parsedArgs.fork || foreignSource),
-						deps.forceSetupWizard === true,
-						showStartupSplash,
-						eventBus,
-						subagentEventBus,
-						initialMessage,
-						initialImages,
-						parsedArgs.join,
-						startBackgroundModelDiscovery,
-						startupLease,
-					);
+					const runTui = (runtime?: StudioHostRuntime) => {
+						// The desktop Host owns the UI. In a non-TTY launch, starting the
+						// interactive editor makes the Runtime treat stdin as an interrupt
+						// source and exit with code 129 immediately after the Bridge handshake.
+						// Keep the Bridge process alive until the desktop sends shutdown.
+						if (isStudioHost && runtime !== undefined && !(process.stdin.isTTY && process.stdout.isTTY)) {
+							return runtime.waitForShutdown().then(async () => {
+								await postmortem.quit(0);
+							});
+						}
+						return runInteractiveMode(
+							session,
+							VERSION,
+							startupChangelog,
+							notifs,
+							versionCheckPromise,
+							initialArgs.messages,
+							setToolUIContext,
+							lspServers,
+							mcpManager,
+							Boolean(parsedArgs.continue || parsedArgs.resume || parsedArgs.fork || foreignSource),
+							deps.forceSetupWizard === true,
+							showStartupSplash,
+							eventBus,
+							subagentEventBus,
+							initialMessage,
+							initialImages,
+							parsedArgs.join,
+							startBackgroundModelDiscovery,
+							startupLease,
+							runtime?.services.loop,
+							runtime?.services.modes,
+							runtime?.waitForShutdown(),
+						);
+					};
+					if (isStudioHost) {
+						let activeToolUIContextSetter = setToolUIContext;
+						const recreateSession = async (sessionFile: string): Promise<AgentSession> => {
+							const reopened = await SessionManager.open(sessionFile, undefined, undefined, {
+								suppressBreadcrumb: true,
+							});
+							const revivedEventBus = new EventBus();
+							const revivedSubagentEventBus = new EventBus();
+							const revived = await createSession({
+								...sessionOptions,
+								cwd: reopened.getCwd(),
+								sessionManager: reopened,
+								eventBus: revivedEventBus,
+								subagentEventBus: revivedSubagentEventBus,
+								...(extensionsResult.preparedExtensions === undefined
+									? {}
+									: { preloadedPreparedExtensions: extensionsResult.preparedExtensions }),
+								...(mcpManager === undefined ? {} : { mcpManager }),
+							});
+							activeToolUIContextSetter = revived.setToolUIContext;
+							return revived.session;
+						};
+						await (deps.runStudioHostMode ?? defaultRunStudioHostMode)(
+							session,
+							{
+								endpoint: parsedArgs.bridgeEndpoint,
+								tokenFile: parsedArgs.bridgeTokenFile,
+								runtimeEpoch: parsedArgs.bridgeRuntimeEpoch,
+							},
+							runTui,
+							{
+								tanCustomTools: mcpManager ? createMCPProxyTools(mcpManager) : undefined,
+								setToolUIContext: (uiContext, hasUI) => activeToolUIContextSetter(uiContext, hasUI),
+								recreateSession,
+							},
+						);
+					} else {
+						await runTui();
+					}
 				} finally {
 					startupLease?.dispose();
 				}
-			} else {
+			} else if (mode === "text" || mode === "json") {
 				// Branch-only single-shot runner: keep print-mode code out of normal interactive startup.
 				stopStartupWatchdog();
 				const runPrintMode: RunPrintMode = (await import("./modes/print-mode")).runPrintMode;
@@ -2114,6 +2201,8 @@ export async function runRootCommand(
 				await session.dispose();
 				stopThemeWatcher();
 				await postmortem.quit(0);
+			} else {
+				throw new Error(`Unsupported non-interactive mode: ${mode}`);
 			}
 		}
 	} catch (error) {
diff --git a/packages/coding-agent/test/main-host-classification.test.ts b/packages/coding-agent/test/main-host-classification.test.ts
index d982d853b8..9124673155 100644
--- a/packages/coding-agent/test/main-host-classification.test.ts
+++ b/packages/coding-agent/test/main-host-classification.test.ts
@@ -25,3 +25,26 @@ it("classifies an interactive host before opening auth storage", async () => {
 
 	expect(observedTimeout).toBe(5000);
 });
+
+it("classifies studio-host as an interactive host before opening auth storage", async () => {
+	const previous = setInteractiveHost(false);
+	const stop = new Error("stop after studio-host classification");
+	let observedTimeout: number | undefined;
+	const parsed = parseArgs(["--mode", "studio-host"]);
+	parsed.noExtensions = true;
+
+	try {
+		await expect(
+			runRootCommand(parsed, ["--mode", "studio-host"], {
+				discoverAuthStorage: async () => {
+					observedTimeout = getDbBusyTimeoutMs();
+					throw stop;
+				},
+			}),
+		).rejects.toBe(stop);
+	} finally {
+		setInteractiveHost(previous);
+	}
+
+	expect(observedTimeout).toBe(5000);
+});
