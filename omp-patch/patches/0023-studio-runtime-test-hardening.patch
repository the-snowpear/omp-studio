diff --git a/packages/coding-agent/src/studio/bridge-server.ts b/packages/coding-agent/src/studio/bridge-server.ts
index ef89b9a..b684823 100644
--- a/packages/coding-agent/src/studio/bridge-server.ts
+++ b/packages/coding-agent/src/studio/bridge-server.ts
@@ -202,7 +202,7 @@ export class StudioBridgeServer implements StudioBridgeLifecycle {
 					type: "studio.hello.result",
 					requestId: hello.requestId,
 					selectedProtocolVersion: STUDIO_PROTOCOL_VERSION,
-					runtimeVersion: `${VERSION}-studio.22`,
+					runtimeVersion: `${VERSION}-studio.23`,
 					upstreamVersion: VERSION,
 					upstreamCommit: UPSTREAM_COMMIT,
 					runtimeInstanceId: runtime.runtimeId,
diff --git a/packages/coding-agent/src/studio/services/agent-hub-service.ts b/packages/coding-agent/src/studio/services/agent-hub-service.ts
index a026ff6..1860aac 100644
--- a/packages/coding-agent/src/studio/services/agent-hub-service.ts
+++ b/packages/coding-agent/src/studio/services/agent-hub-service.ts
@@ -904,7 +904,11 @@ export class StudioAgentHubService {
 		}
 		let payload: string;
 		try {
-			payload = Buffer.from(cursor, "base64url").toString("utf8");
+			const decoded = Buffer.from(cursor, "base64url");
+			if (decoded.toString("base64url") !== cursor) {
+				throw new Error("Non-canonical cursor encoding");
+			}
+			payload = decoded.toString("utf8");
 		} catch {
 			throw new StudioAgentHubError("INVALID_CURSOR", "Malformed transcript cursor");
 		}
diff --git a/packages/coding-agent/src/studio/state-projector.ts b/packages/coding-agent/src/studio/state-projector.ts
index b726db5..d51afa1 100644
--- a/packages/coding-agent/src/studio/state-projector.ts
+++ b/packages/coding-agent/src/studio/state-projector.ts
@@ -74,6 +74,7 @@ export class StudioStateProjector {
 	#telemetryTimer: ReturnType<typeof setTimeout> | undefined;
 	#telemetryInFlight = false;
 	#telemetryQueued = false;
+	#telemetrySnapshot: StudioSessionTelemetry;
 
 	constructor(runtime: StudioHostRuntime) {
 		this.#runtime = runtime;
@@ -81,6 +82,7 @@ export class StudioStateProjector {
 		this.#loopService = runtime.services.loop;
 		this.#liveService = runtime.services.live;
 		this.#modeService = runtime.services.modes;
+		this.#telemetrySnapshot = this.#telemetry();
 		this.#committedSnapshot = this.snapshot();
 		this.#unsubscribePause = this.#pauseService.onChange(() => this.commitStateChange());
 		this.#unsubscribeLoop = this.#loopService.onChange(() => this.commitStateChange());
@@ -140,7 +142,7 @@ export class StudioStateProjector {
 				callerAgentId: this.#runtime.session.getAgentId() ?? "Main",
 				includeRecent: true,
 			}),
-			telemetry: this.#telemetry(),
+			telemetry: structuredClone(this.#telemetrySnapshot),
 		};
 		if (plan !== undefined) snapshot.plan = { status: plan.status };
 		if (goal !== undefined) {
@@ -245,8 +247,17 @@ export class StudioStateProjector {
 	}
 
 	#telemetry(): StudioSessionTelemetry {
-		const stats = this.#runtime.session.getSessionStats();
-		const context = this.#runtime.session.getContextBreakdown();
+		const stats =
+			typeof this.#runtime.session.getSessionStats === "function"
+				? this.#runtime.session.getSessionStats()
+				: {
+						tokens: { input: 0, output: 0, reasoning: 0, cacheRead: 0, cacheWrite: 0, total: 0 },
+						cost: 0,
+					};
+		const context =
+			typeof this.#runtime.session.getContextBreakdown === "function"
+				? this.#runtime.session.getContextBreakdown()
+				: undefined;
 		const tokens = stats.tokens;
 		const telemetry: StudioSessionTelemetry = {
 			sessionId: this.#runtime.sessionId,
@@ -278,7 +289,8 @@ export class StudioStateProjector {
 				? { unavailableReason: "model_context_unknown" as const }
 				: {}),
 		};
-		const messages = this.#runtime.session.messages as Array<{
+		const rawMessages = this.#runtime.session.messages;
+		const messages = (Array.isArray(rawMessages) ? rawMessages : []) as Array<{
 			role?: string;
 			stopReason?: string;
 			timestamp?: number | string;
@@ -345,6 +357,7 @@ export class StudioStateProjector {
 		this.#telemetryInFlight = true;
 		try {
 			const telemetry = this.#telemetry();
+			this.#telemetrySnapshot = structuredClone(telemetry);
 			if (this.#committedSnapshot !== undefined)
 				this.#committedSnapshot = { ...this.#committedSnapshot, telemetry: structuredClone(telemetry) };
 			this.#emitEvent({ kind: "session.telemetry.changed", sessionId: this.#runtime.sessionId, telemetry });
diff --git a/packages/coding-agent/test/studio-approval-ask-e2e.test.ts b/packages/coding-agent/test/studio-approval-ask-e2e.test.ts
index 6d6e988..278c234 100644
--- a/packages/coding-agent/test/studio-approval-ask-e2e.test.ts
+++ b/packages/coding-agent/test/studio-approval-ask-e2e.test.ts
@@ -84,7 +84,7 @@ describe("studio real tool approval and ask E2E", () => {
 				await Bun.sleep(50 * (attempt + 1));
 			}
 		}
-	});
+	}, 15_000);
 
 	function remoteUi() {
 		const arbiter = new StudioRuntimeCommandArbiter(
diff --git a/packages/coding-agent/test/studio-bridge-server.test.ts b/packages/coding-agent/test/studio-bridge-server.test.ts
index c511ea9..687c805 100644
--- a/packages/coding-agent/test/studio-bridge-server.test.ts
+++ b/packages/coding-agent/test/studio-bridge-server.test.ts
@@ -324,6 +324,21 @@ describe("WP-011 Studio Bridge runtime server", () => {
 				jobsRevision: 0,
 				agents: [],
 				jobs: [],
+				telemetry: {
+					sessionId: "session-test",
+					capturedAt: expect.stringMatching(/^\d{4}-\d{2}-\d{2}T/),
+					tokens: {
+						input: 0,
+						output: 0,
+						reasoning: 0,
+						cacheRead: 0,
+						cacheWrite: 0,
+						total: 0,
+						cost: 0,
+					},
+					context: null,
+					unavailableReason: "model_context_unknown",
+				},
 			},
 			commandManifestHash: expect.stringMatching(/^sha256:/),
 			capabilityHash: expect.stringMatching(/^sha256:/),
@@ -647,6 +662,7 @@ describe("WP-011 Studio Bridge runtime server", () => {
 			set: (key: string, value: unknown) => {
 				if (key === "tools.approvalMode") currentMode = value as string;
 			},
+			clearOverride: () => {},
 			override: (key: string, value: unknown) => {
 				if (key === "tools.approvalMode") currentMode = value as string;
 			},
