diff --git a/packages/coding-agent/src/plan-mode/approved-plan.ts b/packages/coding-agent/src/plan-mode/approved-plan.ts
index 942f95b1df..ebdbcda406 100644
--- a/packages/coding-agent/src/plan-mode/approved-plan.ts
+++ b/packages/coding-agent/src/plan-mode/approved-plan.ts
@@ -10,6 +10,7 @@ export interface PlanApprovalDetails {
 	planFilePath: string;
 	title: string;
 	planExists: boolean;
+	planContent?: string;
 }
 
 /** Validate and normalize the agent-supplied plan title into a safe filename stem.
diff --git a/packages/coding-agent/src/registry/agent-registry.ts b/packages/coding-agent/src/registry/agent-registry.ts
index f7337b9b7b..d6792598db 100644
--- a/packages/coding-agent/src/registry/agent-registry.ts
+++ b/packages/coding-agent/src/registry/agent-registry.ts
@@ -50,6 +50,7 @@ export interface AgentRunLifecycle {
 
 /** Historical identity and telemetry that remain available after the live session is disposed. */
 export interface AgentHistorySummary {
+	isolated?: boolean;
 	agent?: string;
 	modelRole?: string;
 	resolvedModel?: string;
@@ -92,6 +93,7 @@ export type RegistryEvent =
 	| { type: "registered"; ref: AgentRef }
 	| { type: "status_changed"; ref: AgentRef }
 	| { type: "metadata_changed"; ref: AgentRef }
+	| { type: "session_changed"; ref: AgentRef; previousSession: AgentSession | null }
 	| { type: "removed"; ref: AgentRef };
 
 type RegistryListener = (event: RegistryEvent) => void;
@@ -266,8 +268,7 @@ export class AgentRegistry {
 	/**
 	 * Record a short activity gist for the work-aware roster. Display-only and
 	 * read on demand (`irc list`, peer roster), so it emits no event — keeping
-	 * the per-tool-call update rate off the registry listener path (same as
-	 * `attachSession`, which also bumps `lastActivity` without emitting). Only a
+	 * the per-tool-call update rate off the registry listener path. Only a
 	 * `running` agent has current work: a heartbeat for any other status is
 	 * dropped, so a late progress flush can't resurrect activity on a ref that
 	 * `setStatus` just cleared. Every running heartbeat refreshes `lastActivity`
@@ -298,16 +299,20 @@ export class AgentRegistry {
 		// closes the race between a parked reviver claiming the ref and finishing
 		// createAgentSession after an explicit kill.
 		if (!ref || ref.status === "aborted" || !this.#matchesExpected(ref, expected)) return false;
+		const previousSession = ref.session;
 		ref.session = session;
 		if (sessionFile !== undefined) ref.sessionFile = sessionFile;
 		ref.lastActivity = Date.now();
+		this.#emit({ type: "session_changed", ref, previousSession });
 		return true;
 	}
 
 	detachSession(id: string, expected?: AgentRefExpectation): boolean {
 		const ref = this.#refs.get(id);
 		if (!ref || !this.#matchesExpected(ref, expected)) return false;
+		const previousSession = ref.session;
 		ref.session = null;
+		this.#emit({ type: "session_changed", ref, previousSession });
 		return true;
 	}
 
diff --git a/packages/coding-agent/src/registry/persisted-agents.ts b/packages/coding-agent/src/registry/persisted-agents.ts
index 02f9cd8aac..fea8b127e3 100644
--- a/packages/coding-agent/src/registry/persisted-agents.ts
+++ b/packages/coding-agent/src/registry/persisted-agents.ts
@@ -350,6 +350,7 @@ async function readPersistedAgentMetadata(
 						typeof record.modelRole === "string" ? record.modelRole : (history.modelRole ?? inferred.modelRole),
 					resolvedModel: typeof record.resolvedModel === "string" ? record.resolvedModel : history.resolvedModel,
 					readOnly: typeof record.readOnly === "boolean" ? record.readOnly : inferred.readOnly,
+					isolated: record.isolated === true,
 				};
 				return false;
 			},
diff --git a/packages/coding-agent/src/session/agent-session-events.ts b/packages/coding-agent/src/session/agent-session-events.ts
index 50152b5979..a2b13cf19a 100644
--- a/packages/coding-agent/src/session/agent-session-events.ts
+++ b/packages/coding-agent/src/session/agent-session-events.ts
@@ -46,6 +46,11 @@ export type AgentSessionEvent =
 			finalError?: string;
 			retryErrors?: RetryErrorUpdate[];
 	  }
+	| {
+			type: "unexpected_stop_retry";
+			attempt: number;
+			maxAttempts: number;
+	  }
 	| { type: "retry_fallback_applied"; from: string; to: string; role: string }
 	| { type: "retry_fallback_succeeded"; model: string; role: string }
 	| { type: "model_changed" }
diff --git a/packages/coding-agent/src/session/agent-session-types.ts b/packages/coding-agent/src/session/agent-session-types.ts
index 28a4e1c177..c18981a745 100644
--- a/packages/coding-agent/src/session/agent-session-types.ts
+++ b/packages/coding-agent/src/session/agent-session-types.ts
@@ -40,6 +40,7 @@ import type { ConfiguredThinkingLevel } from "@oh-my-pi/pi-tui/thinking";
 import type { ToolSession } from "../tools";
 import type { XdevState } from "../tools/xdev";
 import type { CodexAutoRedeemCoordinator } from "./codex-auto-reset";
+import type { CustomMessage } from "./messages";
 import type { SessionManager } from "./session-manager";
 
 /** Maximum time the interactive shutdown path waits for Mnemopi consolidation. */
@@ -354,6 +355,8 @@ export interface PromptOptions {
 	attribution?: MessageAttribution;
 	/** Skip pre-send compaction checks for this prompt. */
 	skipCompactionCheck?: boolean;
+	/** Messages injected ahead of the user message in the same turn. */
+	prependMessages?: CustomMessage[];
 }
 
 /** Payload for {@link AgentSession.setPromptDropped}: a user prompt cancelled
diff --git a/packages/coding-agent/src/session/agent-session.ts b/packages/coding-agent/src/session/agent-session.ts
index 20621db53f..373bd9d16c 100644
--- a/packages/coding-agent/src/session/agent-session.ts
+++ b/packages/coding-agent/src/session/agent-session.ts
@@ -477,6 +477,8 @@ type ScheduledAgentContinueOptions = {
 	shouldContinue?: () => boolean;
 	onSkip?: (reason: AgentContinueSkipReason) => void;
 	onError?: (error: unknown) => void;
+	/** Studio deferred model/thinking: apply before draining queued user follow-ups. */
+	applyBeforeNextUserTurn?: boolean;
 };
 
 type ScheduledAgentContinueRequest = {
@@ -815,6 +817,7 @@ export class AgentSession {
 	 *  incremented, so `isStreaming` alone cannot tell a host that a submission is admitted. */
 	#admittedSubmissionCount = 0;
 	#admittedSubmissionsSettled: PromiseWithResolvers<void> | undefined;
+	#beforeNextUserTurn: (() => Promise<void>) | undefined;
 	// Wire-level agent_end emission deferred until #promptInFlightCount drops to 0.
 	// Internal extension hooks and post-emit work (auto-retry, auto-compaction, todo
 	// checks in #handleAgentEvent) still fire on the original schedule — only the
@@ -1244,6 +1247,10 @@ export class AgentSession {
 		return this.#prewalk.arm(target, thinkingLevel);
 	}
 
+	disarmPrewalk(): boolean {
+		return this.#prewalk.disarm();
+	}
+
 	/** Restore a planning model and re-arm prewalk without partially applying a rejected restart. */
 	restartPrewalk(
 		source: Model,
@@ -1260,7 +1267,11 @@ export class AgentSession {
 		if (!state?.enabled) {
 			throw new ToolError("Plan mode is not active.");
 		}
-		const { planFilePath, title: resolvedTitle } = await resolveApprovedPlan({
+		const {
+			planFilePath,
+			title: resolvedTitle,
+			planContent,
+		} = await resolveApprovedPlan({
 			suppliedTitle: title,
 			statePlanFilePath: state.planFilePath,
 			readPlan: url => this.#readPlanFile(url),
@@ -1268,7 +1279,7 @@ export class AgentSession {
 		});
 		return {
 			content: [{ type: "text", text: "Plan ready for review." }],
-			details: { planFilePath, title: resolvedTitle, planExists: true },
+			details: { planFilePath, title: resolvedTitle, planExists: true, planContent },
 		};
 	}
 
@@ -3831,7 +3842,7 @@ export class AgentSession {
 					return;
 				}
 			}
-			if (signal.aborted) {
+			if (signal.aborted || this.#abortInProgress || this.#isDisposed) {
 				options?.onSkip?.("aborted");
 				return;
 			}
@@ -3942,7 +3953,13 @@ export class AgentSession {
 				// streaming turn — agent.continue() here would race the handoff's session
 				// reset. The first-class fix is in #checkCompaction/the agent_end handler,
 				// but this guard catches anything that bypasses that path.
-				if (signal.aborted || this.#isDisposed || this.isCompacting || this.isGeneratingHandoff) {
+				if (
+					signal.aborted ||
+					this.#isDisposed ||
+					this.#abortInProgress ||
+					this.isCompacting ||
+					this.isGeneratingHandoff
+				) {
 					this.#skipAgentContinue("session-unavailable", request);
 					return;
 				}
@@ -3950,6 +3967,14 @@ export class AgentSession {
 					this.#skipAgentContinue("should-continue-false", request);
 					return;
 				}
+				if (options?.applyBeforeNextUserTurn) {
+					try {
+						await this.#runBeforeNextUserTurn();
+					} catch (error) {
+						options?.onError?.(error);
+						return;
+					}
+				}
 
 				const active = this.#activeAgentContinue;
 				if (active) {
@@ -4009,6 +4034,7 @@ export class AgentSession {
 				delayMs: 100,
 				generation: options.generation,
 				shouldContinue: () => this.agent.hasQueuedMessages(),
+				applyBeforeNextUserTurn: true,
 			});
 			return true;
 		}
@@ -4052,6 +4078,7 @@ export class AgentSession {
 						source: "auto-continue-queued-message",
 						generation,
 						shouldContinue: () => this.agent.hasQueuedMessages(),
+						applyBeforeNextUserTurn: true,
 					});
 					return;
 				}
@@ -4438,6 +4465,19 @@ export class AgentSession {
 		}
 	}
 
+	/**
+	 * Hosts such as Studio apply deferred session model/thinking changes here.
+	 * Invoked at the start of the next user prompt, queued follow-up drain, or
+	 * manual retry — not on 503 auto-retry `agent.continue()`.
+	 */
+	setBeforeNextUserTurn(hook: (() => Promise<void>) | undefined): void {
+		this.#beforeNextUserTurn = hook;
+	}
+
+	async #runBeforeNextUserTurn(): Promise<void> {
+		await this.#beforeNextUserTurn?.();
+	}
+
 	/**
 	 * Subscribe to agent events.
 	 * Session persistence is handled internally (saves messages on message_end).
@@ -6392,7 +6432,10 @@ export class AgentSession {
 				throw new AgentBusyError();
 			}
 
-			// Steer/follow-up/aside the keyword notices BEFORE the queued user message so the
+			for (const prelude of options?.prependMessages ?? []) {
+				await this.#queueCustomMessage(prelude, streamingBehavior);
+			}
+			// Steer/follow-up the keyword notices BEFORE the queued user message so the
 			// model reads the steering notice ahead of the prompt it modifies.
 			for (const notice of keywordNotices) {
 				await this.#queueCustomMessage(notice, streamingBehavior);
@@ -6420,7 +6463,6 @@ export class AgentSession {
 			!options?.synthetic && !hasPendingUserDirective ? this.#todo.createEagerTodoPrelude(expandedText) : undefined;
 		const eagerTaskPrelude =
 			!options?.synthetic && !hasPendingUserDirective ? this.#todo.createEagerTaskPrelude(expandedText) : undefined;
-		const videoAttachmentNotices = this.#createVideoAttachmentNotices(options?.images, submittedAt);
 		const normalizedImages = await this.#normalizeImagesForModel(options?.images);
 
 		const userContent: (TextContent | ImageContent)[] = [{ type: "text", text: expandedText }];
@@ -6494,21 +6536,16 @@ export class AgentSession {
 
 		let dispatched = false;
 		try {
+			const injected = [
+				...(options?.prependMessages ?? []),
+				...preludeMessages,
+				...keywordNotices,
+				...(imageDescriptionNotice ? [imageDescriptionNotice] : []),
+			];
 			dispatched = await this.#promptWithMessage(message, expandedText, {
 				...options,
 				images: normalizedImages,
-				prependMessages:
-					preludeMessages.length > 0 ||
-					keywordNotices.length > 0 ||
-					videoAttachmentNotices.length > 0 ||
-					imageDescriptionNotice
-						? [
-								...preludeMessages,
-								...keywordNotices,
-								...videoAttachmentNotices,
-								...(imageDescriptionNotice ? [imageDescriptionNotice] : []),
-							]
-						: undefined,
+				prependMessages: injected.length > 0 ? injected : undefined,
 			});
 		} catch (error) {
 			if (error instanceof AgentStartPolicyChangedError && message.role === "user") {
@@ -6782,6 +6819,9 @@ export class AgentSession {
 			acceptTerminalEmptyStop?: boolean;
 		},
 	): Promise<boolean> {
+		if (message.role === "user") {
+			await this.#runBeforeNextUserTurn();
+		}
 		// Returns false when the prompt was dropped before reaching the agent —
 		// every pre-dispatch bail (generation bump from abort, disposal, usage
 		// preflight denial) exits silently, and prompt() uses the outcome to hand
@@ -7341,6 +7381,7 @@ export class AgentSession {
 		this.#queuedMessageDrainScheduled = true;
 		this.#scheduleAgentContinue({
 			source: "queued-message-drain",
+			applyBeforeNextUserTurn: true,
 			shouldContinue: () => {
 				this.#queuedMessageDrainScheduled = false;
 				return (
@@ -9140,7 +9181,10 @@ export class AgentSession {
 	}
 
 	/** Retry the last failed assistant turn when the session is idle. */
-	retry(): Promise<boolean> {
+	async retry(): Promise<boolean> {
+		if (!this.isStreaming && !this.isCompacting) {
+			await this.#runBeforeNextUserTurn();
+		}
 		return this.#recovery.retry();
 	}
 
diff --git a/packages/coding-agent/src/session/claude-session-store.ts b/packages/coding-agent/src/session/claude-session-store.ts
index 79807a2479..b2c7e81396 100644
--- a/packages/coding-agent/src/session/claude-session-store.ts
+++ b/packages/coding-agent/src/session/claude-session-store.ts
@@ -115,7 +115,9 @@ async function readRegisteredProjects(root: string): Promise<string[]> {
 }
 
 function projectCwd(encoded: string, registered: readonly string[]): string {
-	const exact = registered.find(project => project.replaceAll(path.sep, "-") === encoded);
+	const exact = registered.find(
+		project => project.replaceAll(path.sep, "-") === encoded || project.replace(/[:/\\._]/g, "-") === encoded,
+	);
 	if (exact) return exact;
 	if (!encoded.startsWith("-")) return encoded;
 	return encoded.replaceAll("-", path.sep);
diff --git a/packages/coding-agent/src/session/prewalk.ts b/packages/coding-agent/src/session/prewalk.ts
index 36d4de9a09..b50e4108e1 100644
--- a/packages/coding-agent/src/session/prewalk.ts
+++ b/packages/coding-agent/src/session/prewalk.ts
@@ -223,6 +223,13 @@ export class PrewalkCoordinator {
 		});
 	}
 
+	/** Clears an armed prewalk without switching models. */
+	disarm(): boolean {
+		if (!this.#prewalk) return false;
+		this.#clearPrewalkState();
+		return true;
+	}
+
 	/** Arms a prewalk immediately for an explicit slash-command request. */
 	arm(target: Model, thinkingLevel?: ConfiguredThinkingLevel): boolean {
 		const active = this.#prewalk;
diff --git a/packages/coding-agent/src/session/session-entries.ts b/packages/coding-agent/src/session/session-entries.ts
index 6c5de984dd..af309fb591 100644
--- a/packages/coding-agent/src/session/session-entries.ts
+++ b/packages/coding-agent/src/session/session-entries.ts
@@ -18,6 +18,10 @@ export const SESSION_TITLE_SLOT_ENTRY_TYPE = "title";
 
 export const TITLE_CHANGE_ENTRY_TYPE = "title_change";
 
+export const STUDIO_HOST_SESSION_ORIGIN = "studio-host";
+
+export type StudioSessionOrigin = typeof STUDIO_HOST_SESSION_ORIGIN;
+
 export type SessionTitleSource = "auto" | "user";
 
 /** Fixed-width first-line slot carrying the mutable current session title. */
@@ -51,6 +55,8 @@ export interface SessionHeader {
 	previousSessionFiles?: string[];
 	/** Provider prompt-cache identity inherited by exact-route full forks. */
 	providerPromptCacheKey?: string;
+	/** Optional creation marker ignored by unpatched OMP readers. */
+	studioOrigin?: StudioSessionOrigin;
 }
 
 export interface NewSessionOptions {
@@ -61,6 +67,10 @@ export interface NewSessionOptions {
 	drop?: boolean;
 	/** Additional workspace directories to seed on the new session. */
 	additionalDirectories?: string[];
+	/** Creation marker for a fresh session; never applied while opening an existing session. */
+	studioOrigin?: StudioSessionOrigin;
+	/** Reset inherited usage totals when forking a session. */
+	resetInheritedCost?: boolean;
 }
 
 export interface SessionEntryBase {
diff --git a/packages/coding-agent/src/session/session-manager.ts b/packages/coding-agent/src/session/session-manager.ts
index 3504b16e85..06fc5994f4 100644
--- a/packages/coding-agent/src/session/session-manager.ts
+++ b/packages/coding-agent/src/session/session-manager.ts
@@ -61,6 +61,7 @@ import {
 	type SessionMessageEntry,
 	type SessionTitleSource,
 	type SessionTreeNode,
+	type StudioSessionOrigin,
 	type ThinkingLevelChangeEntry,
 	TITLE_CHANGE_ENTRY_TYPE,
 	type TitleChangeEntry,
@@ -702,6 +703,9 @@ export class SessionManager {
 	#hasTitleSlot = true;
 	#entries: SessionEntry[] = [];
 	#index = new SessionEntryIndex();
+	#reservedMessageIds: Array<{ role: "user" | "assistant" | "system"; id: string }> = [];
+	#reservedCompactionIds: string[] = [];
+	#reservedMessageIdSet = new Set<string>();
 
 	/** File reflects all current entries; appends can go incrementally. */
 	#fileIsCurrent = false;
@@ -785,6 +789,8 @@ export class SessionManager {
 	 * back to the most-recent session instead of being treated as a fresh crumb.
 	 */
 	#breadcrumbFresh = false;
+	/** Creation marker inherited by future `/new` sessions without mutating the currently opened header. */
+	#newSessionStudioOrigin: StudioSessionOrigin | undefined;
 	#sessionNameChangedCallbacks = new Set<() => void>();
 	#persistenceErrorCallbacks = new Set<(error: Error) => void>();
 
@@ -1485,6 +1491,8 @@ export class SessionManager {
 			parentSession: options?.parentSession,
 			providerPromptCacheKey: options?.providerPromptCacheKey,
 		};
+		const studioOrigin = options?.studioOrigin ?? this.#newSessionStudioOrigin;
+		if (studioOrigin) this.#header.studioOrigin = studioOrigin;
 		const workspace = normalizeSessionWorkspace({
 			cwd: this.#cwd,
 			directories: options?.additionalDirectories ?? [],
@@ -1497,6 +1505,7 @@ export class SessionManager {
 
 		this.#entries = [];
 		this.#index.clear();
+		this.clearReservedMessageIds();
 		this.#fileIsCurrent = false;
 		this.#rewriteRequired = false;
 		this.#forceFileCreation = false;
@@ -1531,16 +1540,80 @@ export class SessionManager {
 		this.#titleSource = header.titleSource;
 		this.#titleUpdatedAt = header.timestamp;
 		this.#index.rebuild(entries);
+		this.clearReservedMessageIds();
+	}
+
+	#occupiedIds(): { has(id: string): boolean } {
+		return { has: id => this.#index.has(id) || this.#reservedMessageIdSet.has(id) };
+	}
+
+	#nextId(): string {
+		return generateId(this.#occupiedIds());
+	}
+
+	#takeReservedMessageId(role: "user" | "assistant" | "system"): string | undefined {
+		const index = this.#reservedMessageIds.findIndex(item => item.role === role);
+		if (index < 0) return undefined;
+		const [reserved] = this.#reservedMessageIds.splice(index, 1);
+		this.#reservedMessageIdSet.delete(reserved.id);
+		return reserved.id;
 	}
 
 	#freshEntryFields(): { id: string; parentId: string | null; timestamp: string } {
 		return {
-			id: generateId(this.#index),
+			id: this.#nextId(),
 			parentId: this.#index.leafId(),
 			timestamp: nowIso(),
 		};
 	}
 
+	/**
+	 * Reserve a SessionEntry id from the same `generateId` pool used by persist.
+	 * `appendMessage` for that role reuses the oldest matching reservation.
+	 */
+	reserveMessageId(role: "user" | "assistant" | "system"): string {
+		const id = this.#nextId();
+		this.#reservedMessageIdSet.add(id);
+		this.#reservedMessageIds.push({ role, id });
+		return id;
+	}
+
+	/** Allocate an entry id without enqueueing it for `appendMessage`. */
+	allocateEntryId(): string {
+		return this.#nextId();
+	}
+
+	/**
+	 * Reserve a SessionEntry id for a forthcoming `appendCompaction`.
+	 * Does not consume the message-id FIFO.
+	 */
+	reserveCompactionId(): string {
+		const id = this.#nextId();
+		this.#reservedMessageIdSet.add(id);
+		this.#reservedCompactionIds.push(id);
+		return id;
+	}
+
+	releaseCompactionId(id: string): void {
+		const index = this.#reservedCompactionIds.indexOf(id);
+		if (index < 0) return;
+		this.#reservedCompactionIds.splice(index, 1);
+		this.#reservedMessageIdSet.delete(id);
+	}
+
+	#takeReservedCompactionId(): string | undefined {
+		const id = this.#reservedCompactionIds.shift();
+		if (id === undefined) return undefined;
+		this.#reservedMessageIdSet.delete(id);
+		return id;
+	}
+
+	clearReservedMessageIds(): void {
+		this.#reservedMessageIds.length = 0;
+		this.#reservedCompactionIds.length = 0;
+		this.#reservedMessageIdSet.clear();
+	}
+
 	#setLeaf(id: string | null): void {
 		this.#index.setLeaf(id);
 		const batch = this.#atomicEntryBatch;
@@ -2504,6 +2577,10 @@ export class SessionManager {
 		return this.#sessionId;
 	}
 
+	setNewSessionStudioOrigin(origin: StudioSessionOrigin | undefined): void {
+		this.#newSessionStudioOrigin = origin;
+	}
+
 	getSessionFile(): string | undefined {
 		return this.#sessionFile;
 	}
@@ -2735,7 +2812,15 @@ export class SessionManager {
 			| PythonExecutionMessage
 			| FileMentionMessage,
 	): string {
-		const entry: SessionMessageEntry = { type: "message", ...this.#freshEntryFields(), message };
+		const role = message.role;
+		const reserved = role === "user" || role === "assistant" ? this.#takeReservedMessageId(role) : undefined;
+		const entry: SessionMessageEntry = {
+			type: "message",
+			...(reserved === undefined
+				? this.#freshEntryFields()
+				: { id: reserved, parentId: this.#index.leafId(), timestamp: nowIso() }),
+			message,
+		};
 		this.#recordEntry(entry);
 		return entry.id;
 	}
@@ -2869,9 +2954,12 @@ export class SessionManager {
 			tokensAfter?: number;
 		} = {},
 	): string {
+		const reserved = this.#takeReservedCompactionId();
 		const entry: CompactionEntry<T> = {
 			type: "compaction",
-			...this.#freshEntryFields(),
+			...(reserved === undefined
+				? this.#freshEntryFields()
+				: { id: reserved, parentId: this.#index.leafId(), timestamp: nowIso() }),
 			summary,
 			shortSummary,
 			firstKeptEntryId,
@@ -3255,10 +3343,16 @@ export class SessionManager {
 	 * @param cwd Working directory (stored in the session header)
 	 * @param sessionDir Optional session directory; defaults to the cwd-derived dir.
 	 */
-	static create(cwd: string, sessionDir?: string, storage: SessionStorage = new FileSessionStorage()): SessionManager {
+	static create(
+		cwd: string,
+		sessionDir?: string,
+		storage: SessionStorage = new FileSessionStorage(),
+		options?: NewSessionOptions,
+	): SessionManager {
 		const dir = sessionDir ?? SessionManager.getDefaultSessionDir(cwd, undefined, storage);
 		const manager = new SessionManager(cwd, dir, true, storage);
-		manager.#resetToNewSession();
+		manager.#newSessionStudioOrigin = options?.studioOrigin;
+		manager.#resetToNewSession(options);
 		return manager;
 	}
 
@@ -3302,6 +3396,7 @@ export class SessionManager {
 			copyArtifacts?: boolean;
 			suppressBreadcrumb?: boolean;
 			sessionFile?: string;
+			studioOrigin?: StudioSessionOrigin;
 			resetInheritedCost?: boolean;
 			repairInterruptedTail?: boolean;
 		},
@@ -3309,6 +3404,7 @@ export class SessionManager {
 		const dir = sessionDir ?? SessionManager.getDefaultSessionDir(cwd, undefined, storage);
 		const manager = new SessionManager(cwd, dir, true, storage);
 		manager.#suppressBreadcrumb = options?.suppressBreadcrumb === true;
+		manager.#newSessionStudioOrigin = options?.studioOrigin;
 
 		// A missing source must fail instead of forking an empty parentless session:
 		// the loader swallows ENOENT by default for fresh-session opens, so fork opts out.
@@ -3331,6 +3427,7 @@ export class SessionManager {
 			{
 				parentSession: sourceHeader?.id,
 				providerPromptCacheKey: sourceHeader?.providerPromptCacheKey ?? sourceHeader?.id,
+				studioOrigin: options?.studioOrigin,
 			},
 			options?.sessionFile,
 		);
@@ -3518,6 +3615,7 @@ export class SessionManager {
 		cwd: string,
 		sessionDir?: string,
 		storage: SessionStorage = new FileSessionStorage(),
+		options?: { studioOrigin?: StudioSessionOrigin },
 	): Promise<SessionManager> {
 		const dir = sessionDir ?? SessionManager.getDefaultSessionDir(cwd, undefined, storage);
 		const resolvedCwd = path.resolve(cwd);
@@ -3537,7 +3635,8 @@ export class SessionManager {
 				(!sessionDir || pathIsWithin(dir, path.dirname(breadcrumb.sessionFile)))
 			) {
 				const manager = new SessionManager(cwd, dir, true, storage);
-				manager.#resetToNewSession();
+				manager.#newSessionStudioOrigin = options?.studioOrigin;
+				manager.#resetToNewSession({ studioOrigin: options?.studioOrigin });
 				return manager;
 			}
 
@@ -3609,8 +3708,9 @@ export class SessionManager {
 		if (chosenSession === undefined) chosenSession = await findMostRecentSession(dir, storage);
 
 		const manager = new SessionManager(cwd, dir, true, storage);
+		manager.#newSessionStudioOrigin = options?.studioOrigin;
 		if (chosenSession) await manager.setSessionFile(chosenSession);
-		else manager.#resetToNewSession();
+		else manager.#resetToNewSession({ studioOrigin: options?.studioOrigin });
 		return manager;
 	}
 
diff --git a/packages/coding-agent/src/session/turn-recovery.ts b/packages/coding-agent/src/session/turn-recovery.ts
index 31aaaaec2f..17fcfc00b5 100644
--- a/packages/coding-agent/src/session/turn-recovery.ts
+++ b/packages/coding-agent/src/session/turn-recovery.ts
@@ -274,6 +274,8 @@ type UsageLimitOutcome = {
 export class TurnRecovery {
 	readonly #host: TurnRecoveryHost;
 	#retryAbortController: AbortController | undefined;
+	/** Sticky cancel so abort wins even if the backoff controller is created after `abortRetry()`. */
+	#retryCancelled = false;
 	#retryAttempt = 0;
 	#requestBodyReadTimeoutRecoveryPromptSequence: number | undefined;
 	#retryPromise: Promise<void> | undefined;
@@ -419,6 +421,7 @@ export class TurnRecovery {
 		this.#unexpectedStopRetryCount = 0;
 		this.#malformedFunctionCallRetryCount = 0;
 		this.#acceptTerminalEmptyStopForPrompt = false;
+		this.#retryCancelled = false;
 	}
 
 	/** Sets whether one terminal empty stop is accepted for the current prompt. */
@@ -984,6 +987,11 @@ export class TurnRecovery {
 			attribution: "agent",
 			timestamp: Date.now(),
 		});
+		await this.#host.emitSessionEvent({
+			type: "unexpected_stop_retry",
+			attempt: this.#unexpectedStopRetryCount,
+			maxAttempts: UNEXPECTED_STOP_MAX_RETRIES,
+		});
 		this.#host.scheduleAgentContinue({
 			source: "unexpected-stop-retry",
 			generation: this.#host.promptGeneration(),
@@ -2181,6 +2189,7 @@ export class TurnRecovery {
 		// not a retry loop, so it runs even when the user disabled retries: it switches
 		// the model once and lets the base turn proceed.
 		if (!retrySettings.enabled && !options?.fireworksFastFallback) return false;
+		if (this.#retryCancelled || this.#host.abortInProgress() || this.#host.isDisposed()) return false;
 		const classifierRefusal = this.isClassifierRefusal(message);
 
 		const generation = this.#host.promptGeneration();
@@ -2513,6 +2522,10 @@ export class TurnRecovery {
 
 		await this.#recordPendingRetryError(message, id, { switchedCredential, switchedModel, delayMs });
 
+		if (this.#retryWasCancelled(generation)) {
+			return await this.#abandonRetry({ announced: this.#retryAttempt > 1 });
+		}
+
 		await this.#host.emitSessionEvent({
 			type: "auto_retry_start",
 			attempt: this.#retryAttempt,
@@ -2534,6 +2547,10 @@ export class TurnRecovery {
 		// pattern instead of re-sampling the same stalled reasoning.
 		this.#maybeInjectThinkingLoopRedirect(id);
 
+		if (this.#retryWasCancelled(generation)) {
+			return await this.#abandonRetry({ announced: true });
+		}
+
 		// Day-scale provider waits (a monthly quota reset parsed from the error
 		// text) exceed the signed 32-bit timer limit; sleepLong chunks the sleep
 		// so the full wait elapses instead of overflowing the timer. `delayMs`
@@ -2547,24 +2564,16 @@ export class TurnRecovery {
 			if (this.#retryAbortController !== retryAbortController) {
 				return false;
 			}
-			// Aborted during sleep - emit end event so UI can clean up
-			const attempt = this.#retryAttempt;
-			this.#retryAttempt = 0;
-			this.#retryAbortController = undefined;
-			await this.#host.emitSessionEvent({
-				type: "auto_retry_end",
-				success: false,
-				attempt,
-				finalError: "Retry cancelled",
-			});
-			this.#clearPendingRetryErrors();
-			this.resolveRetry();
-			return false;
+			return await this.#abandonRetry({ announced: true });
 		}
 		if (this.#retryAbortController === retryAbortController) {
 			this.#retryAbortController = undefined;
 		}
 
+		if (this.#retryWasCancelled(generation)) {
+			return await this.#abandonRetry({ announced: true });
+		}
+
 		// The identity-keyed removal above can miss when a context rebuild
 		// recreated the failed turn's message object between settle and retry
 		// (fresh identity, same failed tail — issue #5382). Agent.continue()
@@ -2578,6 +2587,10 @@ export class TurnRecovery {
 			this.#stripFailedAssistantTail();
 		}
 
+		if (this.#retryWasCancelled(generation)) {
+			return await this.#abandonRetry({ announced: true });
+		}
+
 		// Retry via continue() outside the agent_end event callback chain. A
 		// continuation that still fails locally must close the retry saga —
 		// otherwise auto_retry_end never fires, retryPromise stays pending, and
@@ -2668,11 +2681,37 @@ export class TurnRecovery {
 	 * Cancel in-progress retry.
 	 */
 	abortRetry(): void {
+		this.#retryCancelled = true;
 		this.#retryAbortController?.abort();
-		// Note: _retryAttempt is reset in the catch block of _autoRetry
 		this.resolveRetry();
 	}
 
+	#retryWasCancelled(generation: number): boolean {
+		return (
+			this.#retryCancelled ||
+			this.#host.abortInProgress() ||
+			this.#host.isDisposed() ||
+			this.#host.promptGeneration() !== generation
+		);
+	}
+
+	async #abandonRetry(options: { announced: boolean }): Promise<false> {
+		const attempt = this.#retryAttempt;
+		this.#retryAttempt = 0;
+		this.#retryAbortController = undefined;
+		if (options.announced) {
+			await this.#host.emitSessionEvent({
+				type: "auto_retry_end",
+				success: false,
+				attempt,
+				finalError: "Retry cancelled",
+			});
+		}
+		this.#clearPendingRetryErrors();
+		this.resolveRetry();
+		return false;
+	}
+
 	async #promptAgentWithIdleRetry(messages: AgentMessage[], options?: { toolChoice?: ToolChoice }): Promise<void> {
 		const deadline = Date.now() + 30_000;
 		for (;;) {
@@ -2788,6 +2827,7 @@ export class TurnRecovery {
 		}
 
 		// Reset retry budget for a fresh attempt
+		this.#retryCancelled = false;
 		this.#retryAttempt = 0;
 
 		// Re-attempt the turn
diff --git a/packages/coding-agent/src/task/executor.ts b/packages/coding-agent/src/task/executor.ts
index 8953cc7631..dc7c72829b 100644
--- a/packages/coding-agent/src/task/executor.ts
+++ b/packages/coding-agent/src/task/executor.ts
@@ -3343,7 +3343,7 @@ export async function runSubprocess(options: ExecutorOptions): Promise<SingleRes
 	);
 	// TTL before an adopted idle subagent is parked by the lifecycle manager.
 	// <= 0 disables parking (the session stays live until process teardown).
-	const agentIdleTtlMs = Math.trunc(Number(settings.get("task.agentIdleTtlMs") ?? 420_000) || 0);
+	const agentIdleTtlMs = Math.trunc(Number(settings.get("task.agentIdleTtlMs") ?? 300_000) || 0);
 	const configuredDefaultBudget = Math.max(
 		0,
 		Math.trunc(Number(settings.get("task.softRequestBudget") ?? SOFT_REQUEST_BUDGET.default) || 0),
@@ -3962,6 +3962,10 @@ export async function runSubprocess(options: ExecutorOptions): Promise<SingleRes
 				// even when the workspace was retained for recovery.
 				isolated: worktree !== undefined || undefined,
 			});
+			const registryAgentId = session.getAgentId();
+			if (worktree !== undefined && registryAgentId !== undefined) {
+				AgentRegistry.global().setHistory(registryAgentId, { isolated: true });
+			}
 
 			abortSignal.addEventListener(
 				"abort",
diff --git a/packages/coding-agent/src/task/isolation-runner.ts b/packages/coding-agent/src/task/isolation-runner.ts
index 9abb167c22..d82aea576c 100644
--- a/packages/coding-agent/src/task/isolation-runner.ts
+++ b/packages/coding-agent/src/task/isolation-runner.ts
@@ -85,6 +85,7 @@ export function renderIsolationSummary(context: IsolationSummaryContext): string
 /** Record artifact locations for `agent://` and mark the result as an isolated run. */
 function rememberAgentArtifacts(result: SingleResult): SingleResult {
 	AgentRegistry.global().setHistory(result.id, {
+		isolated: true,
 		outputPath: result.outputPath,
 		patchPath: result.patchPath,
 		branchName: result.branchName,
diff --git a/packages/coding-agent/test/foreign-session-stores.test.ts b/packages/coding-agent/test/foreign-session-stores.test.ts
index 4921dd0b39..c01551dcd8 100644
--- a/packages/coding-agent/test/foreign-session-stores.test.ts
+++ b/packages/coding-agent/test/foreign-session-stores.test.ts
@@ -39,7 +39,7 @@ async function createClaudeFixture(): Promise<{ info: ForeignSessionInfo; store:
 	const root = path.join(tempRoot, ".claude");
 	const cwd = path.join(tempRoot, "project-with-hyphen");
 	const id = "11111111-1111-4111-8111-111111111111";
-	const projectDirectory = cwd.replaceAll(path.sep, "-");
+	const projectDirectory = cwd.replace(/[:/\\._]/g, "-");
 	const sessionPath = path.join(root, "projects", projectDirectory, `${id}.jsonl`);
 	await writeJsonl(path.join(root, "history.jsonl"), [
 		{ sessionId: id, timestamp: 1_767_225_600_000, display: "First prompt", project: cwd },
@@ -98,7 +98,7 @@ describe("ClaudeSessionStore", () => {
 		const cwd = path.join(tempRoot, "my-project.dir");
 		const id = "33333333-3333-4333-8333-333333333333";
 		// No history entry, and a directory name whose "-" separators are ambiguous.
-		await writeJsonl(path.join(root, "projects", cwd.replace(/[/\\._]/g, "-"), `${id}.jsonl`), [
+		await writeJsonl(path.join(root, "projects", cwd.replace(/[:/\\._]/g, "-"), `${id}.jsonl`), [
 			{ type: "file-history-snapshot", timestamp: "2026-01-01T00:00:00.000Z" },
 			{
 				type: "user",
@@ -117,7 +117,7 @@ describe("ClaudeSessionStore", () => {
 	it("bounds cwd discovery to the transcript prefix before using the encoded fallback", async () => {
 		const root = path.join(tempRoot, ".claude");
 		const cwd = path.join(tempRoot, "late-project.dir");
-		const encoded = cwd.replace(/[/\\._]/g, "-");
+		const encoded = cwd.replace(/[:/\\._]/g, "-");
 		const id = "55555555-5555-4555-8555-555555555555";
 		await writeJsonl(path.join(root, "projects", encoded, `${id}.jsonl`), [
 			{ type: "file-history-snapshot", snapshot: "x".repeat(128 * 1024) },
@@ -125,7 +125,7 @@ describe("ClaudeSessionStore", () => {
 		]);
 
 		const info = (await new ClaudeSessionStore(root).list()).find(item => item.id === id);
-		expect(info?.cwd).toBe(encoded.replaceAll("-", path.sep));
+		expect(info?.cwd).toBe(encoded.startsWith("-") ? encoded.replaceAll("-", path.sep) : encoded);
 	});
 
 	it("prefers the history index cwd over the transcript's", async () => {
@@ -135,7 +135,7 @@ describe("ClaudeSessionStore", () => {
 		await writeJsonl(path.join(root, "history.jsonl"), [
 			{ sessionId: id, timestamp: 1_767_225_600_000, display: ".", project: indexedCwd },
 		]);
-		await writeJsonl(path.join(root, "projects", indexedCwd.replaceAll(path.sep, "-"), `${id}.jsonl`), [
+		await writeJsonl(path.join(root, "projects", indexedCwd.replace(/[:/\\._]/g, "-"), `${id}.jsonl`), [
 			{
 				type: "user",
 				uuid: "u",
@@ -157,7 +157,7 @@ describe("ClaudeSessionStore", () => {
 		await writeJsonl(path.join(root, "history.jsonl"), [
 			{ sessionId: id, timestamp: 1_767_225_600_000, display: ".", project: cwd },
 		]);
-		await writeJsonl(path.join(root, "projects", cwd.replaceAll(path.sep, "-"), `${id}.jsonl`), [
+		await writeJsonl(path.join(root, "projects", cwd.replace(/[:/\\._]/g, "-"), `${id}.jsonl`), [
 			{
 				type: "user",
 				uuid: "u",
@@ -232,7 +232,7 @@ describe("ClaudeSessionStore", () => {
 		const root = path.join(tempRoot, ".claude");
 		const cwd = path.join(tempRoot, "legacy");
 		const id = "22222222-2222-4222-8222-222222222222";
-		const sessionPath = path.join(root, ".projects", cwd.replaceAll(path.sep, "-"), `${id}.jsonl`);
+		const sessionPath = path.join(root, ".projects", cwd.replace(/[:/\\._]/g, "-"), `${id}.jsonl`);
 		await writeJsonl(path.join(root, "history.jsonl"), [
 			{ session_id: id, ts: 1_735_689_600_000, text: "Legacy prompt" },
 		]);
@@ -258,7 +258,7 @@ describe("ClaudeSessionStore", () => {
 		const root = path.join(tempRoot, "relocated-claude");
 		const cwd = path.join(tempRoot, "project-with-hyphen");
 		const id = "22222222-2222-4222-8222-333333333333";
-		const encoded = cwd.replaceAll(path.sep, "-");
+		const encoded = cwd.replace(/[:/\\._]/g, "-");
 		process.env.CLAUDE_CONFIG_DIR = root;
 		await Bun.write(path.join(root, ".claude.json"), JSON.stringify({ projects: { [cwd]: {} } }));
 		await writeJsonl(path.join(root, "projects", encoded, `${id}.jsonl`), [
diff --git a/packages/tui/src/status-line/context-usage.ts b/packages/tui/src/status-line/context-usage.ts
index cada59aa91..063d7852f9 100644
--- a/packages/tui/src/status-line/context-usage.ts
+++ b/packages/tui/src/status-line/context-usage.ts
@@ -230,20 +230,18 @@ export function estimateToolSchemaTokens(tools: ToolSchemaSource, tokenizer: Tok
 
 	const fragments: string[] = [];
 	for (const tool of tools) {
-		// Extension-supplied tools may carry a non-string name/description or a
-		// parameters value whose wire schema stringifies to `undefined` (e.g. a
-		// callable schema that escaped normalization). A non-string fragment is
-		// fatal inside the native tokenizer, so only real strings are counted.
-		const name = tool.name;
-		const description = tool.description;
-		const parameters = tool.parameters;
-		if (typeof name === "string") fragments.push(name);
-		if (typeof description === "string") fragments.push(description);
 		try {
+			// Extension/native tools may expose metadata through getters. A broken
+			// optional descriptor must not make the read-only context estimate fail;
+			// skip that descriptor and keep accounting the remaining tools.
+			const name = tool.name;
+			const description = tool.description;
+			if (typeof name === "string") fragments.push(name);
+			if (typeof description === "string") fragments.push(description);
 			const wireTool: AiTool = {
 				name,
 				description,
-				parameters: parameters as AiTool["parameters"],
+				parameters: tool.parameters as AiTool["parameters"],
 			};
 			const wireJson = JSON.stringify(toolWireSchema(wireTool) ?? {});
 			if (typeof wireJson === "string") fragments.push(wireJson);
